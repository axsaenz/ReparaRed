# OpenSpec SDD configuration — generated by sdd-init on 2026-09-01.
# Persistence mode: openspec (file-based only; Engram unavailable in this runtime).
schema: spec-driven

context: |
  Project: ReparaRed — web platform connecting clients with home-repair technicians (course MVP, greenfield).
  Current state: planning-only. PRD.md, TECH-DESIGN.md (approved for MVP), Design.md, BACKLOG.md, adrs/ (20 accepted ADRs). No application code scaffolded yet.
  Planned stack (TECH-DESIGN.md): monorepo with apps/web (Next.js App Router + session BFF, Vercel) and apps/api (NestJS + Fastify, Railway); packages/api-client (generated from OpenAPI) and packages/config (shared lint/TypeScript config).
  Planned data layer: Supabase (PostgreSQL, Auth, Storage) with Prisma; remote state via TanStack Query.
  Architecture rule: all authorization and business rules live in the API; the web app never accesses Supabase tables directly.
  Testing: no test runner exists yet because no application project has been scaffolded; strict_tdd is disabled until a workspace-level test command covers every in-scope project.
  Persistence: openspec file-based (Engram unavailable in this runtime).

strict_tdd: false

# Project discovery (2026-09-01): zero in-scope application projects discovered
# within two directory levels of the workspace root. The only project marker
# found was .opencode/package.json — opencode plugin tooling (single dependency
# @opencode-ai/plugin, no scripts or tests), not an application project.
projects: []

testing:
  detected: "2026-09-01"
  strict_tdd_mode: disabled
  strict_tdd_reason: >-
    No-runner fallback: zero in-scope projects discovered, so no explicit
    workspace-level test command covers every in-scope project.
  workspace_test_command: null
  projects: []
  layers: []
  coverage: []
  quality_tools: []
  note: >-
    Planning-only repository. Re-run sdd-init detection after apps/web and
    apps/api are scaffolded to populate per-project test commands, test layers,
    coverage, and quality tools, then re-evaluate strict_tdd.

rules:
  proposal:
    - Include rollback plan for risky changes
    - Keep proposals aligned with the accepted ADRs in adrs/
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Respect the 20 accepted ADRs; changing one requires a new superseding ADR
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    guidelines:
      - Follow existing code patterns
      - Follow the TECH-DESIGN.md monorepo layout (apps/web, apps/api, packages/)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
