NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
# Runtime pooled database connection
DATABASE_URL=
# Prisma CLI migrations only
DIRECT_URL=

# Future server-only names (2026-09-02); introduced by the planned auth and storage adapters.
# These names are not consumed by the current API and must remain unset until
# the corresponding implementation is merged.
# AUTH_ISSUER_URL=
# AUTH_JWKS_URL=
# STORAGE_SERVICE_KEY=
# STORAGE_BUCKET_NAME=
