# No variables are consumed by the current web application.
# Future BFF names for the planned session and API adapter; keep values in
# platform settings and never commit them.
# API_ORIGIN=
# NEXT_PUBLIC_APP_ENV=
